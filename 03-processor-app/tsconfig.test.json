{
  // Herencia de la configuración base
  "extends": "./tsconfig.json", 
  
  "compilerOptions": {    
    //"rootDir": "./",
    // Agregar tipos de Jest
    "types": ["jest", "node", "aws-lambda"]
  },
  
  // 💡 Incluye tanto el código fuente como las pruebas
  "include": [
    "src/**/*",
    "tests/**/*" 
  ],
  
  // Asegúrate de que node_modules sigue excluido
  "exclude": [
    "node_modules" 
  ]
}