{
  "name": "appointment",
  "version": "1.0.0",
  "description": "gestion de citas",
  "main": "index.js",
  "scripts": {
    "test": "jest --config=jest.config.js --tsConfig=tsconfig.test.json",
    "build": "esbuild src/index.ts --bundle --outfile=dist/main.js --platform=node --target=node22 --external:aws-sdk"
  },
  "author": "Raul Talledo",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.156",
    "@types/jest": "^29.5.0",
    "@types/node": "^24.9.1",
    "esbuild": "^0.25.11",
    "jest": "^29.5.0",
    "pino-lambda": "^4.4.1",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "aws-sdk": "^2.1692.0",
    "pino": "^10.1.0"
  }
}
